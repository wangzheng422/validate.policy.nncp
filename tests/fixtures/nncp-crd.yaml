# AI-Author: Codex (OpenAI model not exposed by runtime)
# Minimal admission fixture preserving the upstream NMState opaque desiredState.
# Source: https://github.com/nmstate/kubernetes-nmstate/blob/v0.81.0/deploy/crds/nmstate.io_nodenetworkconfigurationpolicies.yaml
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: nodenetworkconfigurationpolicies.nmstate.io
spec:
  group: nmstate.io
  scope: Cluster
  names:
    plural: nodenetworkconfigurationpolicies
    singular: nodenetworkconfigurationpolicy
    kind: NodeNetworkConfigurationPolicy
    shortNames: [nncp]
  versions:
  - name: v1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              capture:
                type: object
                additionalProperties:
                  type: string
              desiredState:
                type: object
                x-kubernetes-preserve-unknown-fields: true
              nodeSelector:
                type: object
                additionalProperties:
                  type: string
              maxUnavailable:
                anyOf:
                - type: integer
                - type: string
                x-kubernetes-int-or-string: true
    subresources:
      status: {}
